<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.f6.twodo.mapper.BlogMapper">
    <select id="getBlogMemory" resultType="com.f6.twodo.vo.Blog">
        SELECT *
        FROM blog
        ORDER BY id DESC
    </select>

    <insert id="addBlog">
  INSERT INTO blog(secret, title, content, orifilename , filename, locationtag
     , rank,  insertdate, modifydate)
VALUES (#{blog.secret}, #{blog.title},#{blog.content},#{blog.orifilename},#{blog.filename},#{blog.locationtag},

        #{blog.rank}, now(), now())

    </insert>
</mapper>